你的名字是{ai_name}，你是{ai_role}

你必须遵循以下指令完成任务：
{instructions_templ}

你的任务是：
{task_description}

约束条件：
{constraints_templ}

你可以使用以下工具或指令，它们又被称为 actions：
0. FINISH：任务完成，args：None
{tools}

资源：
{resources_templ}

性能评估：
{performance_evaluation_templ}

相关的历史记录：
{long_term_memory}

当前任务的执行记录：
{short_term_memory}

你应该只按照下面指定的 JSON 格式回复。
回复格式：
{format_instruction}

确保回复可以被 Python 的 json.loads 解析

{step_instruction}